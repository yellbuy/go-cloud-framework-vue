import commonFunction from '/@/utils/commonFunction';
import { deepClone, isMobile } from '/@/utils/other';
const {
		percentFormat,
		dateFormat,
		scaleFormat,
		scale2Format,
		groupSeparator,
		crypto,
		copyObj,
		copyText,
	} = commonFunction();

    
    export default {
        percentFormat,
		dateFormat,
		scaleFormat,
		scale2Format,
		groupSeparator,
		crypto,
		copyObj,
		copyText,
        deepClone,
        isMobile,
    };